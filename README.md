# Nessus_API

## Setup
```
- Configure the Mail configuration section with your valid details
- Configure "base_url" to point to your Nessus instance
- Configure Nessus_Username and Nessus_Password to have your Nessus instance login details.

pip install -r requirements.txt
```
